# Passenger Search API

FastAPI based passenger search application.

## Run locally

Install dependencies:

pip install -r requirements.txt

Start server:

uvicorn app.main:app --reload

API docs:

http://127.0.0.1:8000/docs